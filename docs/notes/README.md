# Notas locales

Puedes guardar aquí apuntes personales (`.txt`, `.md`, etc.). El patrón `docs/notes/**` está en `.gitignore` excepto este archivo, para que la carpeta exista en el repo sin subir tu contenido privado.
