# Recursos para documentación

Imágenes de branding, capturas para README o diagramas sueltos. No es la carpeta de assets del frontend (`frontend/public/`).

- `Akonet.png` — marca actual (AkoeNet).
- `Nexora.png` — imagen de la etapa anterior del nombre (referencia histórica).
