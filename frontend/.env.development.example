# Copia a `.env.development` (no lo subas si contiene datos propios).
# Vite solo carga este archivo en modo development; los builds de producción usan el API de Render por defecto.
VITE_API_URL=http://localhost:3000
